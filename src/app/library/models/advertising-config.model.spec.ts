import { AdvertisingConfig } from './advertising-config.model';

describe('AdvertisingConfig', () => {
  it('should create an instance', () => {
    expect(new AdvertisingConfig()).toBeTruthy();
  });
});
