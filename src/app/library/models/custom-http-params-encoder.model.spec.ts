import { CustomHttpParamsEncoder } from './custom-http-params-encoder.model';

describe('CustomHttpParamsEncoder', () => {
  it('should create an instance', () => {
    expect(new CustomHttpParamsEncoder()).toBeTruthy();
  });
});
