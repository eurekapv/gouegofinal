import { TipoTesseraTemplate } from './tipo-tessera-template';

describe('TipoTesseraTemplate', () => {
  it('should create an instance', () => {
    expect(new TipoTesseraTemplate()).toBeTruthy();
  });
});
