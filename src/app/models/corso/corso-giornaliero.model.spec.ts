import { CorsoGiornaliero } from './corso-giornaliero.model';

describe('CorsoGiornaliero', () => {
  it('should create an instance', () => {
    expect(new CorsoGiornaliero()).toBeTruthy();
  });
});
