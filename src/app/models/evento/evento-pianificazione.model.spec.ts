import { EventoPianificazione } from './evento-pianificazione.model';

describe('EventoPianificazione', () => {
  it('should create an instance', () => {
    expect(new EventoPianificazione()).toBeTruthy();
  });
});
