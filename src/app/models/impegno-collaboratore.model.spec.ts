import { ImpegnoCollaboratore } from './impegno-collaboratore.model';

describe('ImpegnoCollaboratore', () => {
  it('should create an instance', () => {
    expect(new ImpegnoCollaboratore()).toBeTruthy();
  });
});
