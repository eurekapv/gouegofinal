import { IscrizioneTesseramento } from './iscrizione-tesseramento';

describe('IscrizioneTesseramento', () => {
  it('should create an instance', () => {
    expect(new IscrizioneTesseramento()).toBeTruthy();
  });
});
