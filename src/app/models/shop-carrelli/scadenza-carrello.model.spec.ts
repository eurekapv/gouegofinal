import { ScadenzaCarrello } from './scadenza-carrello.model';

describe('ScadenzaCarrello', () => {
  it('should create an instance', () => {
    expect(new ScadenzaCarrello()).toBeTruthy();
  });
});
