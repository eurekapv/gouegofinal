import { AmbitoNewsPipe } from './ambito-news.pipe';

describe('AmbitoNewsPipe', () => {
  it('create an instance', () => {
    const pipe = new AmbitoNewsPipe();
    expect(pipe).toBeTruthy();
  });
});
