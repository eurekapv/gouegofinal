import { ClasseDocumentoPipe } from './classe-documento.pipe';

describe('ClasseDocumentoPipe', () => {
  it('create an instance', () => {
    const pipe = new ClasseDocumentoPipe();
    expect(pipe).toBeTruthy();
  });
});
