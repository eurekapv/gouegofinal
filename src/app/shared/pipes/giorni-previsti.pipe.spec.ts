import { GiorniPrevistiPipe } from './giorni-previsti.pipe';

describe('GiorniPrevistiPipe', () => {
  it('create an instance', () => {
    const pipe = new GiorniPrevistiPipe();
    expect(pipe).toBeTruthy();
  });
});
