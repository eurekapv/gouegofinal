import { LabelGiornoPipe } from './label-giorno.pipe';

describe('LabelGiornoPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelGiornoPipe();
    expect(pipe).toBeTruthy();
  });
});
