import { LabelNumeroOrePipe } from './label-numero-ore.pipe';

describe('LabelNumeroOrePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelNumeroOrePipe();
    expect(pipe).toBeTruthy();
  });
});
