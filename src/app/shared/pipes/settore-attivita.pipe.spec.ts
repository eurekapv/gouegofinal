import { SettoreAttivitaPipe } from './settore-attivita.pipe';

describe('SettoreAttivitaPipe', () => {
  it('create an instance', () => {
    const pipe = new SettoreAttivitaPipe();
    expect(pipe).toBeTruthy();
  });
});
