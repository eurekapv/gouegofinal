import { StatoSlotPipe } from './stato-slot.pipe';

describe('StatoSlotPipe', () => {
  it('create an instance', () => {
    const pipe = new StatoSlotPipe();
    expect(pipe).toBeTruthy();
  });
});
