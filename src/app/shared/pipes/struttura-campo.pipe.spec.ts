import { StrutturaCampoPipe } from './struttura-campo.pipe';

describe('StrutturaCampoPipe', () => {
  it('create an instance', () => {
    const pipe = new StrutturaCampoPipe();
    expect(pipe).toBeTruthy();
  });
});
