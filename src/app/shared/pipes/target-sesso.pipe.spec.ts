import { TargetSessoPipe } from './target-sesso.pipe';

describe('TargetSessoPipe', () => {
  it('create an instance', () => {
    const pipe = new TargetSessoPipe();
    expect(pipe).toBeTruthy();
  });
});
