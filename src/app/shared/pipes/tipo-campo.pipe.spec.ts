import { TipoCampoPipe } from './tipo-campo.pipe';

describe('TipoCampoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoCampoPipe();
    expect(pipe).toBeTruthy();
  });
});
