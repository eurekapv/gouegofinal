import { TipoCorsoPipe } from './tipo-corso.pipe';

describe('TipoCorsoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoCorsoPipe();
    expect(pipe).toBeTruthy();
  });
});
