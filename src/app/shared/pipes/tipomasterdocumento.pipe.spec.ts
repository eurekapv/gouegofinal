import { TipomasterdocumentoPipe } from './tipomasterdocumento.pipe';

describe('TipomasterdocumentoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipomasterdocumentoPipe();
    expect(pipe).toBeTruthy();
  });
});
